{/* Styleguide.stories.mdx*/}

import { Meta, Story } from '@storybook/addon-docs/blocks';
import './Styleguide.scss';

<Meta title="Styleguide" />

# Typography

<Story name="Typography">
  <div className="space-y-lg">
    <div className="flex items-center space-x-sm">
      <code>{`<h1>`}</code>
      <h1>Dados Abertos de Feira</h1>
    </div>
    <div className="flex items-center space-x-sm">
      <code>{`<h2>`}</code>
      <h2>Dados Abertos de Feira</h2>
    </div>
    <div className="flex items-center space-x-sm">
      <code>{`<h3>`}</code>
      <h3>Dados Abertos de Feira</h3>
    </div>
  </div>
</Story>

# Colors

<Story name="Colors">
  <div className="grid grid-cols-4">
    {[
      'primary',
      'primary-dark',
      'primary-light',
      'secondary',
      'secondary-dark',
      'secondary-light',
      'alert',
      'gray-darkest',
      'gray-dark',
      'gray',
      'gray-light',
      'gray-lightest',
      'white',
    ].map((colorName) => (
      <div key={colorName}>
        <div
          className={`bg-${colorName}`}
          style={{
            height: '48px',
            width: '48px',
          }}
        />
        <code>{`.bg-${colorName}`}</code>
      </div>
    ))}
  </div>
</Story>

# Shadows

<Story name="Shadows">
  <div
    style={{
      display: 'grid',
      gridGap: '8px',
      gridTemplateColumns: 'repeat(4, 1fr)',
    }}
  >
    <div>
      <div
        style={{
          background: 'var(--white)',
          boxShadow: 'var(--shadow-1)',
          height: '48px',
          width: '48px',
        }}
      />
      <code>$shadow-1</code>
    </div>
    <div>
      <div
        style={{
          background: 'var(--white)',
          boxShadow: 'var(--shadow-2)',
          height: '48px',
          width: '48px',
        }}
      />
      <code>$shadow-2</code>
    </div>
  </div>
</Story>
